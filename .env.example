# Copy this file to .env.local and fill in real values.
# .env.local is gitignored and must never be committed.

# Local Postgres (matches docker-compose.yml; server falls back to this if unset)
DATABASE_URL=postgres://minorillusion:localdev@localhost:5432/minorillusion
SERVER_PORT=3001

ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=
